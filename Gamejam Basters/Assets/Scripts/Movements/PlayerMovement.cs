using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    private float Speed;
    private float JumpHeight;
    void Start()
    {
        Speed = 5f;
        JumpHeight = 2f;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
